# Як правильно заходити на сайт

Оскільки ваш проєкт налаштований як багатосервісна система, ось інструкція, як отримати до нього доступ з різних пристроїв.

## 1. Запуск сервера
Завжди запускайте файл `run_all.bat` у кореневій папці проєкту. Це запустить усі сервіси (адмінку, замовлення, авторизацію) та головний Шлюз (Gateway).

## 2. Доступ з комп'ютера, де запущений сервер
Використовуйте адресу:
👉 `http://localhost:8000` або `http://127.0.0.1:8000`

## 3. Доступ з іншого комп'ютера або телефону в тій же мережі (Wi-Fi)
Вам потрібно використовувати локальну IP-адресу вашого основного комп'ютера. Наразі це:
👉 **`http://192.168.31.131:8000`**

*(Якщо IP-адреса зміниться, ви можете дізнатися нову, ввівши команду `ipconfig` у терміналі).*

## 4. Чому це важливо?
- **Підтвердження пошти**: Коли ви реєструєтесь, посилання в листі тепер автоматично містить вашу IP-адресу (наприклад, `http://192.168.31.131:8000/verify/...`). Це дозволяє натиснути підтвердження з телефону, і воно спрацює.
- **Мобільність**: Тепер ви можете зайти в адмін-панель або переглянути замовлення з телефону, просто ввівши IP-адресу в браузері мобільного.
